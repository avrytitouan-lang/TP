name: CI

on:
    workflow_dispatch:
    push:
        branches: ["main","fix-worflow"]
    pull_request:
        branches: ["main"]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
        - run: echo "Hello World"

     